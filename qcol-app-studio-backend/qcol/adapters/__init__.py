"""Phase 5 provider-adapter namespace.

The current release deliberately does not import or invoke IBM, Google hardware, or AWS
provider SDKs. The UI records a target provider while the local simulator is
the only execution backend.
"""
